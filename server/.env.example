# server/.env.example
# Copy to .env for local dev. In production these are set via the systemd
# unit's EnvironmentFile, not a .env file — see deploy/env/candle-rider-api.env

PORT=3001
HOST=0.0.0.0
CANDLE_RIDER_DB_PATH=./data/candlerider.db
CLIENT_DIST_PATH=../client/dist
